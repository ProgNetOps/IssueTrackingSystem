@model Employee

@{
    string photoPath = "~/images/" + (Model.PhotoPath ?? "NoImage.jpg");
}

<section>	
    <input asp-for="@Model.Id" hidden />
	<h2 class="m-3 secondaryHeader">@Model.SurName @Model.FirstName</h2>



    <div class="form-group row m-2">
        <label asp-for="PhotoPath" class="col-sm-4 col-form-label text-black"></label>
        <div class="col-sm-8">
            <img src="@photoPath" style="width:100%; height:auto;" asp-append-version="true"/>
        </div>
    </div>

    <div class="form-group row text-center m-3">
        <div class=" col text-center">
            <a class="btn btn-primary" asp-controller="Employee" asp-action="Index">Back To Staff</a>
        </div>
        <div class=" col text-center">
            <a class="btn btn-warning" asp-controller="Employee" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
        </div>
        <div class=" col text-center">
            <a class="btn btn-danger" asp-controller="Employee" asp-action="Delete" asp-route-id="@Model.Id">Delete</a>
        </div>
    </div>

</section>